org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m \
  -XX:+UseG1GC -XX:GCTimeRatio=19 -XX:ParallelGCThreads=4 \
  -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true

# Disable incremental/caching for cross-drive stability
kotlin.incremental=false
kotlin.caching.enabled=false

# Run Kotlin compiler out-of-process
kotlin.compiler.execution.strategy=out-of-process

# Build settings
org.gradle.parallel=false
org.gradle.caching=false
org.gradle.configureondemand=true
org.gradle.workers.max=2
org.gradle.daemon.idletimeout=60000

# R8 runs in separate process (configured in settings.gradle.kts)
android.settings.executionProfile=r8-high-memory