<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/assistantOverlayRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:focusable="true">

    <View
        android:id="@+id/overlayScrim"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:alpha="0"
        android:background="#7A000000" />

    <LinearLayout
        android:id="@+id/assistantOverlaySheet"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:background="@drawable/assistant_overlay_compact_bg"
        android:paddingStart="16dp"
        android:paddingTop="14dp"
        android:paddingEnd="16dp"
        android:paddingBottom="14dp"
        android:clickable="true"
        android:focusable="true"
        android:elevation="22dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Zero Two Assistant"
                android:textColor="#FFF7F7F7"
                android:textSize="14sp"
                android:textStyle="bold" />

            <ImageButton
                android:id="@+id/overlayCloseButton"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:background="@android:color/transparent"
                android:contentDescription="Close"
                android:padding="8dp"
                android:src="@android:drawable/ic_menu_close_clear_cancel"
                android:tint="#B3FFFFFF" />
        </LinearLayout>

        <TextView
            android:id="@+id/overlayStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Ready"
            android:textColor="#FFFFFFFF"
            android:textSize="13sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/overlayTranscript"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginTop="8dp"
            android:background="#141A23"
            android:padding="12dp"
            android:text="Tap mic for voice-to-voice or type a command."
            android:textColor="#E6FFFFFF"
            android:textSize="14sp" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageButton
                android:id="@+id/overlayMicButton"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="@drawable/assistant_overlay_action_btn_bg"
                android:contentDescription="Voice command"
                android:src="@android:drawable/ic_btn_speak_now"
                android:tint="#FFFF6A6A" />

            <EditText
                android:id="@+id/overlayInput"
                android:layout_width="0dp"
                android:layout_height="44dp"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:background="@drawable/assistant_overlay_input_bg"
                android:hint="Type command..."
                android:imeOptions="actionSend"
                android:inputType="textCapSentences"
                android:maxLines="1"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"
                android:textColor="#FFFFFFFF"
                android:textColorHint="#80FFFFFF"
                android:textSize="14sp" />

            <ImageButton
                android:id="@+id/overlaySendButton"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/assistant_overlay_action_btn_bg"
                android:contentDescription="Send text command"
                android:src="@android:drawable/ic_menu_send"
                android:tint="#FFFF6A6A" />
        </LinearLayout>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Auto closes after action"
            android:textColor="#99FFFFFF"
            android:textSize="11sp" />
    </LinearLayout>
</FrameLayout>
